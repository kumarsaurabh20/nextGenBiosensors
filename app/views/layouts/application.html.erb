<!DOCTYPE html>
<html lang="en" xml:lang="en" xmlns="http://www.w3.org/1999/xhtml">
  <head>
    <title><%= title %></title>
    <%= render 'layouts/stylesheets' %>
    <%= jqgrid_javascripts %>
    <%= jqgrid_stylesheets %>
    <%= javascript_include_tag 'jquery', 'jquery_ujs', 'jquery_migrate', 'application' %>
     <%= csrf_meta_tag %>
    
  </head>
  <body>
    <div class="container">
	  <%= render 'layouts/header' %>
	  <% #<p style="color: green"><%= notice ></p>  %>
      <div id="content" class="round">
        <% flash.each do |key, value| %>
          <div class="flash <%= key %>"><%= value %></div>
        <% end %>

        <%= yield %>

      </div>
	  <%= render 'layouts/footer' %>
	  <%= debug(params) if Rails.env.development? && signed_in_and_master? %>
    </div>
  </body>
</html>
